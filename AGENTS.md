# AGENTS.md

## Cursor Cloud specific instructions

This repository ("BossMind") is currently empty — it contains only a `README.md` placeholder. No application code, services, dependencies, build system, linter, or test framework have been added yet.

### Current state

- **Source code:** None
- **Dependencies:** None (`package.json`, `requirements.txt`, etc. do not exist)
- **Build/lint/test commands:** None configured
- **Services:** None to start

### For future agents

Once application code is added, update this section with:
- How to install dependencies (e.g. `npm install`, `pip install -r requirements.txt`)
- How to run lint, tests, and the dev server
- Any non-obvious environment setup caveats
